#include <stdio.h>
#include <string.h>
#include <stdbool.h>
#include <ctype.h>
int main()
{
    int a[100][100] = {0};
    int n = 0;
    scanf("%d", &n);
    for (int i = 0; i < n; i++)
    {
        }
}